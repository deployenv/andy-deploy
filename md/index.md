# devops-gitlab

<div style="display:flex;align-items:center;gap:10px;">
  <code>bash &lt;(curl -sL devopsandy.hdyauto.qzz.io/devops.sh?$(openssl rand -base64 12 | tr -dc 'a-zA-Z0-9' | cut -c1-16)) setup_gitlab</code>
  <button onclick="navigator.clipboard.writeText('bash <(curl -sL devopsandy.hdyauto.qzz.io/devops.sh?$(openssl rand -base64 12 | tr -dc 'a-zA-Z0-9' | cut -c1-16)) setup_gitlab')">
    复制
  </button>
</div>

# devops-gitlab

<div style="display:flex;align-items:center;gap:10px;">
  <code>bash &lt;(curl -sL devopsandy.hdyauto.qzz.io/devops.sh?$(openssl rand -base64 12 | tr -dc 'a-zA-Z0-9' | cut -c1-16)) setup_desktop</code>
  <button onclick="navigator.clipboard.writeText('bash <(curl -sL devopsandy.hdyauto.qzz.io/devops.sh?$(openssl rand -base64 12 | tr -dc 'a-zA-Z0-9' | cut -c1-16)) setup_desktop')">
    复制
  </button>
</div>

# main

<div style="display:flex;align-items:center;gap:10px;">
  <code>bash &lt;(curl -sL devopsandy.hdyauto.qzz.io/menu/main)</code>
  <button onclick="navigator.clipboard.writeText('bash <(curl -sL devopsandy.hdyauto.qzz.io/menu/main)')">
    复制
  </button>
</div>

| 菜单 | 地址                                         | 操作 | 描述         |
| ---- | -------------------------------------------- | ---- | ------------ |
| 菜单 | bash <(curl -sL devopsandy.hdyauto.qzz.io/menu/main) | 复制 | 展现所有操作 |
